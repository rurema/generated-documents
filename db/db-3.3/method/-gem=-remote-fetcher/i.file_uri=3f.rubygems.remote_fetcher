names=file_uri?
visibility=public
kind=defined
source_location=manual/api/rubygems/remote_fetcher/Gem__RemoteFetcher.md
since_by_name=file_uri=3f=1.9.3
until_by_name=

### def file_uri?(uri) -> bool

"file://" で始まる文字列である場合は真を返します。そうでない場合は偽を返します。

- **param** `uri` -- URI を表す文字列を指定します。

