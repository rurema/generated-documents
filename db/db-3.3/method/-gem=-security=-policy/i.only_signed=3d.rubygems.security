names=only_signed=
visibility=public
kind=defined
source_location=manual/api/rubygems/security.md
since_by_name=only_signed=3d=1.9.3
until_by_name=

### def only_signed=(flag)

署名付きの Gem のみインストールするかどうかを設定します。

- **param** `flag` -- 真、または偽を指定します。

