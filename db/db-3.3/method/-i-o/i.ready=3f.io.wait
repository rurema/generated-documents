names=ready?
visibility=public
kind=added
source_location=manual/api/io/wait.md
since_by_name=
until_by_name=ready=3f=4.0
rbs_sig=(uninitialized)

### def ready? -> bool | nil

ブロックせずに読み込み可能ならtrueを、
ブロックしてしまう可能性があるならfalseを返します。

判定不可能な場合は nil を返します。

