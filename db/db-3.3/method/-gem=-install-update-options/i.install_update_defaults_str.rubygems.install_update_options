names=install_update_defaults_str
visibility=public
kind=defined
source_location=manual/api/rubygems/install_update_options.md
since_by_name=install_update_defaults_str=1.9.3
until_by_name=

### def install_update_defaults_str -> String

Gem コマンドの install サブコマンドに渡されるデフォルトのオプションを返します。

デフォルトのオプションは以下の通りです。

```text
--rdoc --no-force --no-test --wrappers
```

