names=locked?,mu_locked?
visibility=public
kind=defined
source_location=manual/api/mutex_m.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def mu_locked? -> bool
### def locked? -> bool

self がロックされている時、真を返します。

