names=isutf8
visibility=public
kind=added
source_location=manual/api/kconv.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def isutf8 -> bool

self が UTF-8 なバイト列として正当であるかどうかを判定します。

[`Kconv.isutf8(self)`](m:Kconv?.isutf8) と同じです。

