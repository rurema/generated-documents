names=getwd,pwd
visibility=public
kind=defined
source_location=manual/api/_builtin/Dir.md

### def getwd    -> String
### def pwd      -> String

カレントディレクトリのフルパスを文字列で返します。

- **raise** `Errno::EXXX` -- カレントディレクトリの取得に失敗した場合に発生します(が、普通は失敗することはありません)。

```ruby title="例"
Dir.chdir("/tmp")   #=> 0
Dir.getwd           #=> "/tmp"
```

