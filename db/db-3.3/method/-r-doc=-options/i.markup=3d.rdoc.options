names=markup=
visibility=public
kind=defined
source_location=manual/api/rdoc/options.md
since_by_name=markup=3d=2.0.0
until_by_name=

### def markup=(val)

コマンドライン引数の --markup オプションと同様の指定を行います。

- **param** `val` -- フォーマットを文字列で指定します。

