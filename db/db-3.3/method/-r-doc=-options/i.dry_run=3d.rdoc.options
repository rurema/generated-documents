names=dry_run=
visibility=public
kind=defined
source_location=manual/api/rdoc/options.md
since_by_name=dry_run=3d=1.9.3
until_by_name=

### def dry_run=(val)

val に true を指定した場合、コマンドライン引数の --dry-run オプションと
同様の指定を行います。

- **param** `val` -- --dry-run オプションと同じ指定を行う場合は true、そうでない
           場合は false を指定します。

