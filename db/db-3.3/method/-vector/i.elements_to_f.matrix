names=elements_to_f
visibility=public
kind=defined
source_location=manual/api/matrix/Vector.md
since_by_name=elements_to_f=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def elements_to_f -> Vector

ベクトルの各成分を[c:Float]に変換したベクトルを返します。

このメソッドは deprecated です。`map(&:to_f)` を使ってください。

```ruby title="例"
require 'matrix'

p Vector[2, 3, 5].elements_to_f
# => Vector[2.0, 3.0, 5.0]
```

