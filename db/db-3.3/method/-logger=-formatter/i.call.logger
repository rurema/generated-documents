names=call
visibility=public
kind=defined
source_location=manual/api/logger/Logger__Formatter.md
since_by_name=
until_by_name=

### def call(severity, time, progname, msg) -> String

ログ情報をフォーマットして返します。

- **param** `severity` -- ログレベル。

- **param** `time` -- 時間。[c:Time] クラスのオブジェクト。

- **param** `progname` -- プログラム名

- **param** `msg` -- メッセージ。


