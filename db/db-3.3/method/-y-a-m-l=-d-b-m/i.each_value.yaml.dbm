names=each_value
visibility=public
kind=defined
source_location=manual/api/yaml/dbm.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def each_value {|val| ... } -> YAML::DBM

値を引数としてブロックを評価します。

自身を返します。

