names=normalize_yaml_input
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md

### def normalize_yaml_input(input) -> String

YAML 形式の gemspec を正しくフォーマットします。

- **param** `input` -- 文字列か [c:IO] オブジェクトを指定します。

