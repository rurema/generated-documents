names=httponly
visibility=public
kind=defined
source_location=manual/api/cgi/cookie.md
since_by_name=httponly=3.0
until_by_name=httponly=4.0
rbs_sig=(uninitialized)

### def httponly -> bool

自身がhttpオンリークッキーである場合は、真を返します。
そうでない場合は、偽を返します。

