names=as_json
visibility=public
kind=added
source_location=manual/api/json/add/symbol.md
since_by_name=as_json=1.9.3
until_by_name=as_json=4.1
rbs_sig=(uninitialized)

### def as_json(*args) -> Hash
{: since="1.9.3"}

`self` を JSON 形式の文字列に変換する際に使う、中間表現となるハッシュに変換して返します。
[m:Symbol#to_json] が内部で使用しています。

ハッシュには、[m:JSON.create_id] をキーとしてクラス名が、`'s'` に `self` を文字列化した
値([m:Symbol#to_s] の返り値)が入ります。

- **param** `args` -- 無視されます。

```ruby title="例"
require 'json/add/symbol'

hash = :foo.as_json
p hash['json_class'] # => "Symbol"
p hash['s']           # => "foo"
```

- **SEE** [m:Symbol#to_json], [m:Symbol.json_create]
