names=<<,add,push
visibility=public
kind=defined
source_location=manual/api/rexml/parent.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def add(object) -> ()
### def push(object) -> ()
### def <<(object) -> ()

object を子ノード列の最後に追加します。

object の親ノードには self が設定されます。

- **param** `object` -- 追加するノード

