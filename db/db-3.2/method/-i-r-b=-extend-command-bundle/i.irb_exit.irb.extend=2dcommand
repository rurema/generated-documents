names=irb_exit
visibility=public
kind=defined
source_location=manual/api/irb/extend-command.md
since_by_name=
until_by_name=irb_exit=3.4

### def irb_exit(ret = 0) -> object

irb を終了します。ret で指定したオブジェクトを返します。

- **param** `ret` -- 戻り値を指定します。

ユーザが直接使用するものではありません。

