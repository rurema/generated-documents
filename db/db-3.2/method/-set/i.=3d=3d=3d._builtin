names====,include?,member?
visibility=public
kind=defined
source_location=manual/api/_builtin/Set.md
since_by_name==3d=3d=3d=2.5.0
until_by_name=
rbs_sig=(uninitialized)

### def include?(o) -> bool
### def member?(o) -> bool
### def ===(o) -> bool

オブジェクト o がその集合に属する場合に true を返します。

- **param** `o` -- オブジェクトを指定します。

```ruby
set = Set['hello', 'world']
p set.include?('world') # => true
p set.include?('bye')   # => false
```

