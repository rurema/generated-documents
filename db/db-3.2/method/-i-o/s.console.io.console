names=console
visibility=public
kind=added
source_location=manual/api/io/console.md
since_by_name=console=1.9.3
until_by_name=

### def console -> File | nil

端末を [c:File] オブジェクトで返します。

```ruby
require "io/console"
p IO.console # => #<File:/dev/tty>
```

プロセスが端末から切り離された状態で実行すると nil を返します。

戻り値はプラットフォームや環境に依存します。
