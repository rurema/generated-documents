names=write
visibility=public
kind=defined
source_location=manual/api/rubygems/package/tar_writer.md
since_by_name=write=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def write(data) -> Integer

与えられたデータを自身に関連付けられた IO に書き込みます。

- **param** `data` -- 書き込むデータを指定します。

- **return** -- 書き込んだデータのサイズを返します。

