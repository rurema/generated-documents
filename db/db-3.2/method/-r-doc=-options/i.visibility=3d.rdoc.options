names=visibility=
visibility=public
kind=defined
source_location=manual/api/rdoc/options.md
since_by_name=visibility=3d=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def visibility=(val)

コマンドライン引数の --visibility オプションと同様の指定を行います。

- **param** `val` -- :public、:protected、:private のいずれかを指定します。

