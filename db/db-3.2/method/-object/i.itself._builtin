names=itself
visibility=public
kind=defined
source_location=manual/api/_builtin/Object.md
since_by_name=itself=2.2.0
until_by_name=

### def itself -> object

self を返します。

```ruby
string = 'my string' # => "my string"
p string.itself.object_id == string.object_id # => true
```

