names=[]=
visibility=public
kind=defined
source_location=manual/api/cgi/session.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def []=(key, val)

指定されたキーの値を設定します。

- **param** `key` -- キーを指定します。

- **param** `val` -- 値を指定します。

