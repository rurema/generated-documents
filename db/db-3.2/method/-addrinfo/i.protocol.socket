names=protocol
visibility=public
kind=defined
source_location=manual/api/socket/Addrinfo.md
since_by_name=protocol=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def protocol -> Integer

ソケットプロトコルを整数で返します。

```ruby
require 'socket'

p Addrinfo.tcp("localhost", 80).protocol == Socket::IPPROTO_TCP # => true
```

