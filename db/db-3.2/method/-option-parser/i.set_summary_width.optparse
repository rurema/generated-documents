names=set_summary_width,summary_width=
visibility=public
kind=defined
source_location=manual/api/optparse/OptionParser.md
since_by_name=set_summary_width=3.0
until_by_name=
rbs_sig=(uninitialized)

### def summary_width=(width)
### def set_summary_width(width)

サマリを表示するときの幅を整数で指定します。

- **param** `width` -- サマリを表示するときの幅を整数で指定します。

```ruby title="例"
require "optparse"

opts = OptionParser.new do |opts|
  opts.on_head("-i", "--init")
  opts.on("-u", "--update")
  opts.on_tail("-h", "--help")
end

p opts.summary_width   # => 32
p opts.summarize
# => ["    -i, --init\n", "    -u, --update\n", "    -h, --help\n"]
opts.summary_width = 8 # => 8
p opts.summary_width   # => 8
p opts.summarize
# => ["    -i\n", "        --init\n", "    -u\n", "        --update\n", "    -h\n", "        --help\n"]
```

