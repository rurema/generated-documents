names=try_lock
visibility=public
kind=defined
source_location=manual/api/thread/Mutex.md
since_by_name=try_lock=2.3.0
until_by_name=

### def try_lock -> bool

mutex をロックしようとして、ロックが成功した場合、真を返します。
ロックできなかった場合にはブロックせず偽を返します。

```ruby title="例"
m = Thread::Mutex.new
p m.try_lock # => true
p m.try_lock # => false
```

