names=add_extra_args
visibility=public
kind=defined
source_location=manual/api/rubygems/command.md
since_by_name=add_extra_args=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def add_extra_args(args) -> Array

$HOME/.gemrc から読み込んだ引数を追加します。

- **param** `args` -- 追加する引数を配列で指定します。

