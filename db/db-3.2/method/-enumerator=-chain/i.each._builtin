names=each
visibility=public
kind=defined
source_location=manual/api/_builtin/Enumerator__Chain.md

### def each(*args) { |*args| ...} -> object
### def each(*args) -> Enumerator

まず最初の繰り返し可能なオブジェクトの each メソッドを
args 引数とともに呼び出した後、続く繰り返し可能なオブジェクト
も同様に呼び出します。

ブロックが渡されない場合は [c:Enumerator] を返します。

