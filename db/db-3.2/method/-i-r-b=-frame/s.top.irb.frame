names=top
visibility=public
kind=defined
source_location=manual/api/irb/frame.md

### def top(n = 0) -> Binding

上から n 番目のコンテキストを取り出します。

- **param** `n` -- 取り出すコンテキストを [c:Integer] で指定します。n は 0 が最
         上位になります。

