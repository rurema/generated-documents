names=split_all
visibility=public
kind=added
source_location=refm/api/src/rake/core_ext:285

--- split_all(path) -> Array

与えられたパスをディレクトリごとに分割します。

@param path 分割するパスを指定します。

例:
   split_all("a/b/c") # =>  ['a', 'b', 'c']



