names=zero
visibility=public
kind=defined
source_location=manual/api/matrix/Vector.md

### def Vector.zero(size) -> Vector

零ベクトルを返します。

```ruby title="例"
require 'matrix'
p Vector.zero(3) # => Vector[0, 0, 0]
```

- **param** `size` -- ベクトルの次元

