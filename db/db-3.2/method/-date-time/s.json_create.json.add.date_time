names=json_create
visibility=public
kind=added
source_location=manual/api/json/add/date_time.md

### def json_create(hash) -> DateTime

JSON のオブジェクトから Ruby のオブジェクトを生成して返します。

- **param** `hash` -- 適切なキーを持つハッシュを指定します。

