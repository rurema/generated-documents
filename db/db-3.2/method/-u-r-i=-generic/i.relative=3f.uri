names=relative?
visibility=public
kind=defined
source_location=refm/api/src/uri/Generic:363

--- relative?    -> bool

自身に scheme が設定されていないなら true を返します。そうでない場合は false を返します。

例:
  require 'uri'
  p URI.parse('http://example.com/').relative?    #=> false
  p URI.parse('./').relative?                     #=> true

