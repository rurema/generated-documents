filename=memory_view
macro=false
private=false
type=bool
name=rb_memory_view_is_row_major_contiguous
params=(const rb_memory_view_t *view)
source_location=manual/capi/memory_view.md


メモリビュー `view` が行指向 contiguous 配列であれば `true` を返します。

そうでなければ `false` を返します。

