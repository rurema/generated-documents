names=waiting_job?
visibility=public
kind=defined
source_location=refm/api/src/shell/process-controller.rd:110

--- waiting_job?(job) -> bool

指定されたジョブが存在する場合は真を返します。
そうでない場合は偽を返します。

@param job ジョブを指定します。

