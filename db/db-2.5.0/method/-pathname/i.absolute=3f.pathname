names=absolute?
visibility=public
kind=defined
source_location=refm/api/src/pathname.rd:299

--- absolute? -> bool
self が絶対パス指定であれば真を返します。

//emlist[例][ruby]{
require "pathname"

pathname = Pathname("/path/to/example.rb")
pathname.absolute? # => true
pathname = Pathname("../")
pathname.absolute? # => false
//}

