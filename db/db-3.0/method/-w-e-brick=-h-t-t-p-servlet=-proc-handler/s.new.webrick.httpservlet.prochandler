names=new
visibility=public
kind=defined
source_location=manual/api/webrick/httpservlet/prochandler.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def WEBrick::HTTPServlet::ProcHandler.new(proc) -> WEBrick::HTTPServlet::ProcHandler

自身を初期化します。

- **param** `proc` -- [c:Proc] オブジェクトを与えます。クライアントからのリクエストがあった時、
            proc.call(request, response) のように呼び出されます。
            request, response はそれぞれ [c:WEBrick::HTTPRequest] オブジェクトと
            [c:WEBrick::HTTPResponse] オブジェクトです。

