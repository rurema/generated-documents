names=binmode?
visibility=public
kind=defined
source_location=manual/api/_builtin/ARGF.class.md
since_by_name=binmode=3f=1.9.1
until_by_name=
rbs_sig=(uninitialized)

### def binmode? -> bool
{: since="1.9.1"}

ARGF の入力ストリームがバイナリモードなら true を返します。
そうでない場合、false を返します。

バイナリモードにするためには [m:ARGF.class#binmode] を使用します。

```ruby
p ARGF.binmode? # => false
ARGF.binmode
p ARGF.binmode? # => true
```

- **SEE** [m:IO#binmode?], [m:ARGF.class#binmode]

