names=standalone=
visibility=public
kind=defined
source_location=manual/api/rexml/xmldecl.md
since_by_name=
until_by_name=

### def standalone=(value)

スタンドアロン文書であるかどうかを "yes" "no" で設定します。

この属性を省略したい場合は nil を指定します。

- **param** `value` -- 設定値(文字列)

