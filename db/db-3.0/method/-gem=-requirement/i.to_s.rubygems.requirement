names=to_s
visibility=public
kind=defined
source_location=manual/api/rubygems/requirement.md
since_by_name=to_s=2.5.0
until_by_name=
rbs_sig=(uninitialized)

### def to_s -> String

条件を表す文字列を返します。

```ruby
req = Gem::Requirement.new(["< 5.0", ">= 1.9"])
p req.to_s # => "< 5.0, >= 1.9"
```

