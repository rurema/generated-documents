names=nprocessors
visibility=public
kind=defined
source_location=manual/api/etc/Etc.md

### module_function def nprocessors -> Integer

有効な CPU コア数を返します。

```ruby
require 'etc'
p Etc.nprocessors #=> 4
```

