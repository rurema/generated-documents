names=OP_TLS_ROLLBACK_BUG
visibility=public
kind=defined
source_location=manual/api/openssl/SSL.md
since_by_name=
until_by_name=

### const OP_TLS_ROLLBACK_BUG -> Integer

バージョンロールバック攻撃の検出を無効にします。

[m:OpenSSL::SSL::SSLContext#options=] で利用します。

通常は使わないでください。

鍵交換時に、クライアントとサーバで利用可能なプロトコルのバージョンをやりとりします。クライアントによってはサーバが送ったバージョンを無視します。そのようなクライアントと通信する必要がある場合このオプションを有効にします。

