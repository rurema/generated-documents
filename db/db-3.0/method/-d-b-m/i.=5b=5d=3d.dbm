names=[]=
visibility=public
kind=defined
source_location=manual/api/dbm/DBM.md
since_by_name=
until_by_name==5b=5d=3d=3.1
rbs_sig=(uninitialized)

### def []=(key, value)

key をキーとして、value を格納します。
value として nil を指定すると、key に対する要素を削除します。

- **param** `key` --   キー。
- **param** `value` -- 値。

