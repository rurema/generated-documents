names=each,each_pair
visibility=public
kind=defined
source_location=manual/api/dbm/DBM.md
since_by_name=
until_by_name=each=3.1,each_pair=3.1
rbs_sig=(uninitialized)

### def each {|key, value|  ...  } -> self
### def each_pair {|key, value|  ...  } -> self

各要素に対するイテレータ。

