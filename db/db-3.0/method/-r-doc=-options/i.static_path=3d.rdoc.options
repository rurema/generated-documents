names=static_path=
visibility=public
kind=defined
source_location=manual/api/rdoc/options.md
since_by_name=static_path=3d=2.0.0
until_by_name=
rbs_sig=(uninitialized)

### def static_path=(vals)

コマンドライン引数の --copy-files オプションと同様の指定を行います。

- **param** `vals` -- パスを文字列の配列で指定します。

