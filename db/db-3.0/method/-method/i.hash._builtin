names=hash
visibility=public
kind=defined
source_location=manual/api/_builtin/Method.md
since_by_name=hash=
until_by_name=
rbs_sig=(uninitialized)

### def hash    -> Integer
{: since=""}

自身のハッシュ値を返します。

```ruby title="例"
a = method(:==)
b = method(:eql?)
p a.eql? b          # => true
p a.hash == b.hash  # => true
p [a, b].uniq.size  # => 1
```

