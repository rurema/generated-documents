names=winsize=
visibility=public
kind=added
source_location=manual/api/io/console.md
since_by_name=winsize=3d=1.9.3
until_by_name=

### def winsize=(size)

端末のサイズを設定します。

- **param** `size` -- [rows, columns] を数値の配列で指定します。

効果はプラットフォームや環境に依存します。

