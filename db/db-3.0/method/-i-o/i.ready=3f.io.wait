names=ready?
visibility=public
kind=added
source_location=manual/api/io/wait.md
since_by_name=
until_by_name=

### def ready? -> IO | false | nil

ブロックせずに読み込み可能なら真を、
ブロックしてしまう可能性があるならfalseを返します。

判定不可能な場合は nil を返します。

