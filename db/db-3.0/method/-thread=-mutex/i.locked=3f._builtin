names=locked?
visibility=public
kind=defined
source_location=manual/api/thread/Mutex.md

### def locked? -> bool
mutex がロックされている時、真を返します。

```ruby title="例"
m = Mutex.new
p m.locked? # => false
m.lock
p m.locked? # => true
```

