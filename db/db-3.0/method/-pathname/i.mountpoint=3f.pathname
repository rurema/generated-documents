names=mountpoint?
visibility=public
kind=defined
source_location=manual/api/pathname.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def mountpoint? -> bool

self がマウントポイントであれば真を返します。

```ruby title="例"
require "pathname"

path = Pathname("/")
path.mountpoint? # => true
path = Pathname("/usr")
path.mountpoint? # => false
```

