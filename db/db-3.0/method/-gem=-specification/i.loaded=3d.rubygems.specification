names=loaded=
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md
since_by_name=loaded=3d=1.9.3
until_by_name=

### def loaded=(flag)

この Gem パッケージの gemspec が既にロードされているかどうかをセットします。

この属性は永続化しません。

- **param** `flag` -- 既にロードされている場合は真を指定します。

