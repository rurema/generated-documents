names=attributes
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md
since_by_name=attributes=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def Gem::Specification.attributes(*args) -> ()

複数の属性を一度に作成するために使用します。

各属性のデフォルト値は nil になります。

- **param** `args` -- 属性名を一つ以上指定します。

