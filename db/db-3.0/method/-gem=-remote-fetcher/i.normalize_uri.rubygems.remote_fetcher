names=normalize_uri
visibility=public
kind=defined
source_location=manual/api/rubygems/remote_fetcher/Gem__RemoteFetcher.md
since_by_name=normalize_uri=1.9.3
until_by_name=

### def normalize_uri(uri) -> String

URI のスキーム部分が欠けている場合に "http://" を補って返します。

- **param** `uri` -- URI 文字列を指定します。

