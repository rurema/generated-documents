names=compile_error
visibility=private
kind=defined
source_location=manual/api/ripper.md
since_by_name=compile_error=1.9.3
until_by_name=

### def compile_error(msg) -> nil

解析した Ruby プログラムの中にコンパイルエラーがあった場合に実行されます。

- **param** `msg` -- エラーメッセージ。

サブクラスでオーバライドして使用します。

