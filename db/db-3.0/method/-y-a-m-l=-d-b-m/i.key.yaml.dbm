names=key
visibility=public
kind=defined
source_location=manual/api/yaml/dbm.md
since_by_name=key=2.0.0
until_by_name=
rbs_sig=(uninitialized)

### def key(value) -> String | nil

value を持つ要素のキーを返します。

対応するキーが見つからなかった場合は nil を返します。

- **param** `value` -- 検索したい値を指定します。YAML 形式に変換できるオブジェクトが指定できます。

