names=notes
visibility=public
kind=defined
source_location=manual/api/rdoc/markdown.md
since_by_name=notes=2.0.0
until_by_name=
rbs_sig=(uninitialized)

### def notes -> bool

脚注の出力を有効にするかどうかを返します。

デフォルトでは true を返します。

