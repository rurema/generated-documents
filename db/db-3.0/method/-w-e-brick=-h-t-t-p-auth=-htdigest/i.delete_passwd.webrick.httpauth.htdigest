names=delete_passwd
visibility=public
kind=defined
source_location=manual/api/webrick/httpauth/htdigest.md
since_by_name=
until_by_name=

### def delete_passwd(realm, user)
realm に属するユーザ user のパスワードを削除します。

- **param** `realm` -- レルムを指定します。

- **param** `user` -- ユーザ名を指定します。

