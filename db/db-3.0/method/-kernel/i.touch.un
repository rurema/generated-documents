names=touch
visibility=private
kind=added
source_location=manual/api/un.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def touch -> ()

ファイルのアクセス時刻と修正時刻を現在の時刻に変更します。

```console
ruby -run -e touch -- [OPTION] FILE

-v          詳細表示
```

- **SEE** [man:touch(1)]

