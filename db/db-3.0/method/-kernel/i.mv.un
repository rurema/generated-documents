names=mv
visibility=private
kind=added
source_location=manual/api/un.md
since_by_name=
until_by_name=

### def mv -> ()

ファイルを移動します (ファイル名を変更します)。

```console
ruby -run -e mv -- [OPTION] SOURCE DEST

-v          詳細表示
```

- **SEE** [man:mv(1)]

