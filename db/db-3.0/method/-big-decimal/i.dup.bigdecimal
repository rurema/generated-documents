names=dup
visibility=public
kind=defined
source_location=manual/api/bigdecimal/BigDecimal.md
since_by_name=dup=2.5.0
until_by_name=
rbs_sig=(uninitialized)

### def dup -> BigDecimal

self を返すように、BigDecimal で定義されています。

