names=as_json
visibility=public
kind=added
source_location=manual/api/json/add/date.md
since_by_name=as_json=1.9.3
until_by_name=as_json=4.1
rbs_sig=(uninitialized)

### def as_json(*args) -> Hash
{: since="1.9.3"}

`self` を JSON 形式の文字列に変換する際に使う、中間表現となるハッシュに変換して返します。
[m:Date#to_json] が内部で使用しています。

ハッシュには、[m:JSON.create_id] をキーとしてクラス名が、`'y'` に年([m:Date#year])、
`'m'` に月([m:Date#month])、`'d'` に日([m:Date#day])、`'sg'` に改暦日
([m:Date#start])が入ります。

- **param** `args` -- 無視されます。

```ruby title="例"
require 'json/add/date'

hash = Date.new(2024, 1, 2).as_json
p hash['json_class'] # => "Date"
p hash['y']           # => 2024
p hash['m']           # => 1
p hash['d']           # => 2
p hash['sg']          # => 2299161.0
```

- **SEE** [m:Date#to_json], [m:Date.json_create]
