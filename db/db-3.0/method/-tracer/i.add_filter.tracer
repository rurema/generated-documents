names=add_filter
visibility=public
kind=defined
source_location=manual/api/tracer.md
since_by_name=
until_by_name=add_filter=3.1

### def add_filter(p = proc)
@todo

フィルターを追加します。

- **param** `p` -- [c:Proc] オブジェクトを指定します。

