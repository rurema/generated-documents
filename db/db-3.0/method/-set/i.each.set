names=each
visibility=public
kind=defined
source_location=manual/api/set/Set.md

### def each {|o| ... } -> self

集合の各要素についてブロックを実行します。

```ruby
require 'set'
s = Set[10, 20]
ary = []
s.each {|num| ary << num + 1}
p ary # => [11, 21]
```

