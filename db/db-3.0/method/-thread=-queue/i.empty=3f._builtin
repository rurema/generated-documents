names=empty?
visibility=public
kind=defined
source_location=manual/api/thread/Queue.md

### def empty? -> bool

キューが空の時、真を返します。

```ruby title="例"
q = Queue.new
q.empty? # => true
q.push(:resource)
q.empty? # => false
```

