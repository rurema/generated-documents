names=length,size
visibility=public
kind=defined
source_location=manual/api/thread/Queue.md

### def length -> Integer
### def size -> Integer

キューの長さを返します。

```ruby title="例"
q = Queue.new

[:resource1, :resource2, :resource3, nil].each { |r| q.push(r) }

q.length # => 4
```

