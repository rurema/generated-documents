names=add_schedule
visibility=public
kind=defined
source_location=refm/api/src/shell/process-controller.rd:55

--- add_schedule(command) -> ()

指定されたコマンドを待機ジョブとして登録します。
ただし、実行中のジョブが存在しない場合は、そのジョブを直ちに実行します。

@param command コマンドを指定します。

