names=group
visibility=public
kind=defined
source_location=refm/api/src/_builtin/Thread:681

--- group    -> ThreadGroup

スレッドが属している [[c:ThreadGroup]] オブジェクトを返します。

    p Thread.current.group == ThreadGroup::Default
    # => true

