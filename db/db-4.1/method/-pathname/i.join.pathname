names=join
visibility=public
kind=defined
source_location=manual/api/pathname.md
since_by_name=
until_by_name=
rbs_sig={"overloads":[{"segments":[["t","(*"],["c","String"],["t"," | _ToStr | "],["c","Pathname"],["t"," args) -> "],["c","Pathname"]],"params":["args"],"arity":[0,null]}]}

### def join(*args) -> Pathname

与えられたパス名を連結します。

- **param** `args` -- 連結したいディレクトリ名やファイル名を文字列で与えます。

```ruby title="例"
require "pathname"

path0 = Pathname("/usr")                # Pathname:/usr
path0 = path0.join("bin/ruby")          # Pathname:/usr/bin/ruby
    # 上記の path0 の処理は下記の path1 と同様のパスになります
path1 = Pathname("/usr") + "bin/ruby"   # Pathname:/usr/bin/ruby
path0 == path1                          # => true
```

