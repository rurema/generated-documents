names=handles?
visibility=public
kind=defined
source_location=manual/api/rubygems/command.md
since_by_name=handles=3f=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def handles?(args) -> bool

与えられた引数リストを処理することが出来れば真を返します。処理できない場合は偽を返します。

- **param** `args` -- 引数リストを指定子明日。

