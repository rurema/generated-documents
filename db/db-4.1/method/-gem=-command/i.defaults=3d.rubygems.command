names=defaults=
visibility=public
kind=defined
source_location=manual/api/rubygems/command.md
since_by_name=defaults=3d=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def defaults=(hash)

コマンドで使用するデフォルトのオプションをセットします。

- **param** `hash` -- オプションをハッシュで指定します。

