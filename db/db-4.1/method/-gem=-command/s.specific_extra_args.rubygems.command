names=specific_extra_args
visibility=public
kind=defined
source_location=manual/api/rubygems/command.md
since_by_name=specific_extra_args=1.9.3
until_by_name=

### def specific_extra_args(cmd) -> Array

与えられたコマンドに対応する追加の引数を返します。

特別な追加引数はプログラムの開始時に Gem の設定ファイルから読み込まれます。

- **param** `cmd` -- コマンド名を指定します。

