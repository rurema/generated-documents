names=+,union,|
visibility=public
kind=defined
source_location=manual/api/_builtin/Set.md
since_by_name=
until_by_name=
rbs_sig={"overloads":[{"segments":[["t","(_Each[A]) -> self"]],"params":[],"arity":[1,1]}]}

### def union(enum) -> Set
### def +(enum) -> Set
### def |(enum) -> Set

和集合、すなわち、2 つの集合の少なくともどちらか一方に属するすべての要素からなる新しい集合を作ります。

- **param** `enum` -- each メソッドが定義されたオブジェクトを指定します。
- **raise** `ArgumentError` -- 引数 enum に each メソッドが定義されていない場合に発生します。

```ruby
p Set[10, 20, 30] + Set[10, 20, 40]
# => Set[10, 20, 30, 40]
```

