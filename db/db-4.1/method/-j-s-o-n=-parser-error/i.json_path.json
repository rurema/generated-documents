names=json_path
visibility=public
kind=defined
source_location=manual/api/json/JSON__ParserError.md
since_by_name=json_path=4.1
until_by_name=
rbs_sig=(uninitialized)

### def json_path -> String | nil

パースエラーが発生した位置を、JSON ドキュメント中の位置を表す JSONPath 形式の文字列
(例: `$.foo[0].bar`)で返します。キーが重複しているというエラーの場合は、
重複しているキーの位置を指します。

```ruby title="例"
require "json"

begin
  JSON.parse('{"articles": [ { "title": invalid } ]}')
rescue JSON::ParserError => error
  p error.json_path # => "$.articles[0].title"
end
```


