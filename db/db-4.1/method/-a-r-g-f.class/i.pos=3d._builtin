names=pos=
visibility=public
kind=defined
source_location=manual/api/_builtin/ARGF.class.md
since_by_name=pos=3d=
until_by_name=

### def pos=(n)
{: since=""}

ARGFが開いているファイルのファイルポインタを指定位置に移動します。

- **param** `n` -- 先頭からのオフセットをバイト単位の整数で指定します。

```ruby
ARGF.pos = 17
p ARGF.gets # => "This is line two\n"
```

- **SEE** [m:IO#pos=], [m:ARGF.class#pos]

