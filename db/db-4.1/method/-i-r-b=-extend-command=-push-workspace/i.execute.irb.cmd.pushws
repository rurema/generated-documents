names=execute
visibility=public
kind=defined
source_location=manual/api/irb/cmd/pushws.md
since_by_name=
until_by_name=

### def execute(*obj) -> [IRB::WorkSpace]

UNIX シェルコマンドの pushd と同じです。

- **param** `obj` -- [c:IRB::WorkSpace] オブジェクトを指定します。複数指定した
           場合は先頭のオブジェクトのみが設定されます。

