names=nonzero?
visibility=public
kind=defined
source_location=refm/api/src/bigdecimal/BigDecimal:736

--- nonzero? -> self | nil

self が 0 以外のときに self を返します。0 のときに nil を返します。

//emlist[][ruby]{
require "bigdecimal"
BigDecimal("0").nonzero? # => nil
BigDecimal("1").nonzero? # => 0.1e1
//}

