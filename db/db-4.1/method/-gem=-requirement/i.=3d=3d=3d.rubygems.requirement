names====,=~,satisfied_by?
visibility=public
kind=defined
source_location=manual/api/rubygems/requirement.md
since_by_name==3d=3d=3d=2.5.0,=3d=7e=2.5.0,satisfied_by=3f=1.9.3
until_by_name=
rbs_sig=[[["t","("],["c","Gem::Version"],["t"," version) -> bool"]]]

### def satisfied_by?(version) -> bool
### def ===(version) -> bool
### def =~(version) -> bool

引数 version が自身に含まれる全ての必要条件を満たす場合に true を返します。
そうでなければ、false を返します。

- **param** `version` -- [c:Gem::Version] のインスタンスを指定します。

```ruby
req = Gem::Requirement.new("~> 3.2.1")

p req.satisfied_by?(Gem::Version.new('3.2.9'))  # => true
p req.satisfied_by?(Gem::Version.new('3.3.0'))  # => false
```

