names=title=
visibility=public
kind=defined
source_location=manual/api/rdoc/options.md
since_by_name=title=3d=1.9.3
until_by_name=

### def title=(string)

ドキュメントのタイトルがまだ設定されていない場合に string で指定した文
字列に設定します。

コマンドライン引数で既に --title オプションが指定されていた場合には、そ
ちらを優先します。

- **param** `string` -- 設定するタイトルを文字列で指定します。


