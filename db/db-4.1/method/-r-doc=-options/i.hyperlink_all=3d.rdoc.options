names=hyperlink_all=
visibility=public
kind=defined
source_location=manual/api/rdoc/options.md
since_by_name=hyperlink_all=3d=1.9.3
until_by_name=

### def hyperlink_all=(val)

val に true を指定した場合、コマンドライン引数の --hyperlink-all オプションと同様の指定を行います。

- **param** `val` -- --hyperlink-all オプションと同じ指定を行う場合は true、そう
           でない場合は false を指定します。

