names=truncate
visibility=public
kind=defined
source_location=manual/api/stringio.md
since_by_name=
until_by_name=
rbs_sig={"overloads":[{"segments":[["t","("],["c","Integer"],["t",") -> 0"]],"params":[],"arity":[1,1]}]}

### def truncate(len)    -> Integer

自身のサイズが len になるように、自身を切り詰め、もしくは拡大します。
拡大した場合は、その部分を 0 で埋めます。
len を返します。

- **param** `len` -- 変更したいサイズを整数で指定します。

- **raise** `IOError` -- 自身が書き込み可能でない時に発生します。

- **raise** `Errno::EINVAL` -- len がマイナスの時に発生します。

```ruby title="例"
require "stringio"
a = StringIO.new("hoge", 'r+')
a.truncate(2)
p a.string                     # => "ho"
a.truncate(5)
p a.string                     # => "ho\000\000\000"
```

