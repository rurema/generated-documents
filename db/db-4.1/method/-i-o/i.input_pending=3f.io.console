names=input_pending?
visibility=public
kind=added
source_location=manual/api/io/console.md
since_by_name=input_pending=3f=4.1
until_by_name=
rbs_sig=(uninitialized)

### def input_pending? -> bool

`self` から、ブロックせずに読み込めるデータがあるかどうかを返します。


