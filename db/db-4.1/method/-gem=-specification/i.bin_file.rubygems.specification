names=bin_file
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md
since_by_name=bin_file=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def bin_file(name) -> String
{: since="1.9.3"}

この gem に含まれる実行可能ファイル `name` へのフルパスを返します。

- **param** `name` -- 実行可能ファイル名を指定します。

- **SEE** [m:Gem::Specification#bin_dir]

