names=each,each_pair
visibility=public
kind=defined
source_location=refm/api/src/yaml/dbm.rd:71

--- each                        -> YAML::DBM
--- each_pair {|key, val| ... } -> YAML::DBM

自身のキーと値を引数としてブロックを評価します。

自身を返します。

