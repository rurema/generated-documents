names=systmpdir
visibility=public
kind=defined
source_location=manual/api/etc/Etc.md
since_by_name=systmpdir=1.9.3
until_by_name=

### module_function def systmpdir -> String | nil

システムのテンポラリディレクトリを返します。

```ruby
require 'etc'
p Etc.systmpdir # => "/tmp"
```

