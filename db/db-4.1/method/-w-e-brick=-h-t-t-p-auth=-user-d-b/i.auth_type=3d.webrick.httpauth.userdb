names=auth_type=
visibility=public
kind=defined
source_location=manual/api/webrick/httpauth/userdb.md
since_by_name=
until_by_name=

### def auth_type=(type)

認証のタイプをセットします。

- **param** `type` -- [c:WEBrick::HTTPAuth::BasicAuth], [c:WEBrick::HTTPAuth::DigestAuth] のいずれかを指定します。

