names=strip_hashes
visibility=public
kind=defined
source_location=manual/api/rdoc/text.md
since_by_name=strip_hashes=1.9.3
until_by_name=

### def strip_hashes(text) -> String

引数から各行の行頭の # を削除します。

- **param** `text` -- 文字列を指定します。

