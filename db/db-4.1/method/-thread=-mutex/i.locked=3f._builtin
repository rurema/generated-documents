names=locked?
visibility=public
kind=defined
source_location=manual/api/thread/Mutex.md
since_by_name=locked=3f=2.3.0
until_by_name=

### def locked? -> bool
mutex がロックされている時、真を返します。

```ruby title="例"
m = Thread::Mutex.new
p m.locked? # => false
m.lock
p m.locked? # => true
```

