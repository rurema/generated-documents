names=max=
visibility=public
kind=defined
source_location=manual/api/thread/SizedQueue.md
since_by_name=max=3d=2.1.0
until_by_name=

### def max=(n)

キューの最大サイズを設定します。

- **param** `n` -- キューの最大サイズを指定します。

```ruby title="例"
q = Thread::SizedQueue.new(4)
p q.max # => 4
q.max = 5
p q.max # => 5
```

