names=dot,inner_product
visibility=public
kind=defined
source_location=refm/api/src/matrix/Vector:245

--- inner_product(v) -> Float
--- dot(v) -> Float

ベクトル v との内積を返します。

@param v 内積を求めるベクトル

@raise ExceptionForMatrix::ErrDimensionMismatch 自分自身と引数のベクト
       ルの要素の数(次元)が異なっていたときに発生します。

