names=private_instance_methods
visibility=public
kind=defined
source_location=manual/api/_builtin/Module.md
since_by_name=
until_by_name=
rbs_sig={"overloads":[{"segments":[["t","(?boolish include_super) -> ::"],["c","Array"],["t","["],["c","Symbol"],["t","]"]],"params":["include_super"],"arity":[0,1]}]}

### def private_instance_methods(inherited_too = true) -> [Symbol]

そのモジュールで定義されている private メソッド名の一覧を配列で返します。

- **param** `inherited_too` -- false を指定するとそのモジュールで定義されているメソッドのみ返します。

- **SEE** [m:Object#private_methods], [m:Module#instance_methods], [m:Module#public_instance_methods], [m:Module#protected_instance_methods]

```ruby title="例"
module Taggable
  def foo; end
  private def bar; end
end

module Sortable
  include Taggable

  def baz; end
  private def qux; end
end

p Sortable.private_instance_methods # => [:qux, :bar]
p Sortable.private_instance_methods(false) # => [:qux]
```

