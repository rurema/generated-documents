names=build
visibility=public
kind=defined
source_location=manual/api/rubygems/ext/rake_builder.md
since_by_name=build=1.9.3
until_by_name=

### def Gem::Ext::RakeBuilder.build(extension, directory, dest_path, results) -> Array
@todo

mkrf_conf が存在する場合は、それを実行してから Rake を実行します。

- **param** `extension` -- ファイル名を指定します。

- **param** `directory` -- このメソッドでは使用していません。

- **param** `dest_path` -- ???

- **param** `results` -- コマンドの実行結果を格納します。破壊的に変更されます。

- **SEE** [m:Gem::Ext::Builder.make]
