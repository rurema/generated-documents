names=min
visibility=public
kind=defined
source_location=manual/api/io/console.md
since_by_name=min=4.1
until_by_name=
rbs_sig=(uninitialized)

### def min -> Integer | nil

入力操作 (read) 時に受信したい最小のバイト数(termios の VMIN)を返します。

termios を利用できない環境(Windows)では nil を返します。

- **SEE** [m:IO#raw]

