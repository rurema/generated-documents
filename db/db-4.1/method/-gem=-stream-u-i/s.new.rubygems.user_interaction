names=new
visibility=public
kind=defined
source_location=manual/api/rubygems/user_interaction/Gem__StreamUI.md
since_by_name=new=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def Gem::StreamUI.new(in_stream, out_stream, err_stream = STDERR)

このクラスを初期化します。

- **param** `in_stream` -- 入力元のストリームを指定します。

- **param** `out_stream` -- 出力先のストリームを指定します。

- **param** `err_stream` -- エラー出力を指定します。

