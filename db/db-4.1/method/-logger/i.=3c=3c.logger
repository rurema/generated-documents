names=<<
visibility=public
kind=defined
source_location=manual/api/logger/Logger.md

### def <<(msg) -> Integer | nil

ログを出力します。

- **param** `msg` -- ログに出力するメッセージ。

```ruby title="例"
require 'logger'
logger = Logger.new(STDOUT)

logger << "add message"

# => add message
```


