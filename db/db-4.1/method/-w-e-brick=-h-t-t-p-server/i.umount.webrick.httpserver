names=umount,unmount
visibility=public
kind=defined
source_location=refm/api/src/webrick/httpserver/HTTPServer:92

--- unmount(dir)    -> ()
--- umount(dir)     -> ()

サーバ上のディレクトリ dir とサーブレットとの対応を解消します。

@param dir 対応を解消するディレクトリを指定します。

  srv.mount('/img')

