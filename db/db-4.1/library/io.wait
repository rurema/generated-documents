requires=
classes=
methods=IO/i.wait.io.wait,IO/i.wait_writable.io.wait,IO/i.wait_priority.io.wait
sublibraries=
is_sublibrary=false
category=I/O
source_location=manual/api/io/wait.md

IOが読み込み可能になるまで待つ機能を提供するライブラリです。

Windowsではこのライブラリで定義されているメソッドは
Socketに対してしか利用できません。
