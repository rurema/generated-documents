filename=object.c
macro=false
private=false
type=VALUE
name=rb_obj_dup
params=(VALUE obj)
source_location=manual/capi/object.c.md


[m:Object#dup] の実体です。

Cのソースコード上でオブジェクトの複製を作成して返します。

```c title="例"
VALUE 
return_copy(VALUE arg) 
{
    VALUE copy = rb_obj_dup(arg); // 引数で受け取ったオブジェクトを複製し、copyに代入。
    return copy;
}
```

