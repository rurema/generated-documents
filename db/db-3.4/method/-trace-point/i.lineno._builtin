names=lineno
visibility=public
kind=defined
source_location=manual/api/_builtin/TracePoint.md
since_by_name=lineno=2.0.0
until_by_name=
rbs_sig=(uninitialized)

### def lineno -> Integer

発生したイベントの行番号を返します。

- **raise** `RuntimeError` -- イベントフックの外側で実行した場合に発生します。

```ruby title="例"
def foo(ret)
  ret
end
trace = TracePoint.new(:call, :return) do |tp|
  tp.lineno
end
trace.enable
p foo 1
# => 1
# 3
```

