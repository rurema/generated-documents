names=sign
visibility=public
kind=defined
source_location=manual/api/rubygems/security.md
since_by_name=sign=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def sign(data)
自身に設定済みのダイジェストアルゴリズムを用いて与えられたデータに署名します。

- **param** `data` -- 署名対象のデータを指定します。

