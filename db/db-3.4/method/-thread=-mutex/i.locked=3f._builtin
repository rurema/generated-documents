names=locked?
visibility=public
kind=defined
source_location=manual/api/thread/Mutex.md

### def locked? -> bool
mutex がロックされている時、真を返します。

```ruby title="例"
m = Mutex.new
m.locked? # => false
m.lock
m.locked? # => true
```

