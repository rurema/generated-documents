names=locked?
visibility=public
kind=defined
source_location=refm/api/src/thread/Mutex

--- locked? -> bool
mutex がロックされている時、真を返します。

//emlist[例][ruby]{
m = Mutex.new
m.locked? # => false
m.lock
m.locked? # => true
//}

