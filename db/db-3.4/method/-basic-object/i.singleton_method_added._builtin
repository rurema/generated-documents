names=singleton_method_added
visibility=private
kind=defined
source_location=manual/api/_builtin/BasicObject.private_methods_from_Object.md
since_by_name=singleton_method_added=1.9.3
until_by_name=

### def singleton_method_added(name) -> object

特異メソッドが追加された時にインタプリタから呼び出されます。

通常のメソッドの追加に対するフックには
[m:Module#method_added]を使います。

- **param** `name` -- 追加されたメソッド名が [c:Symbol] で渡されます。

```ruby title="例"
class Foo
  def singleton_method_added(name)
    puts "singleton method \"#{name}\" was added"
  end
end

obj = Foo.new
def obj.foo
end

#=> singleton method "foo" was added
```

- **SEE** [m:Module#method_added],[m:BasicObject#singleton_method_removed],[m:BasicObject#singleton_method_undefined]

