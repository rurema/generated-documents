names=*
visibility=public
kind=defined
source_location=manual/api/_builtin/Integer.md
since_by_name==2a=2.4.0
until_by_name=
rbs_sig=(uninitialized)

### def *(other) -> Numeric

`self` に `other` を掛けた値（＝積）を返します。

`Integer` オブジェクトを左項とする算術演算子 `*` はこのメソッドの呼び出しになります。

- **param** `other` -- `self` に対する乗数

```ruby
p 2 * 3 # => 6
```

