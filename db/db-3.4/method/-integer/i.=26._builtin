names=&
visibility=public
kind=defined
source_location=manual/api/_builtin/Integer.md
since_by_name==26=2.4.0
until_by_name=
rbs_sig=(uninitialized)

### def &(other) -> Integer

`self` と `other` のビット論理積を返します。

`Integer` オブジェクトを左項とする二項演算子 `&` はこのメソッドの呼び出しになります。

- **param** `other` -- 数値

```ruby
p 1 & 1  # => 1
p 2 & 3  # => 2
```

