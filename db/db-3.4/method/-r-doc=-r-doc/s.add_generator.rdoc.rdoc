names=add_generator
visibility=public
kind=defined
source_location=manual/api/rdoc/RDoc__RDoc.md
since_by_name=add_generator=1.9.3
until_by_name=

### def RDoc::RDoc.add_generator(klass) -> klass

引数 klass で指定したクラスをジェネレータとして登録します。

- **param** `klass` -- 登録するクラスを指定します。

- **SEE** [ref:c:RDoc::Options#custom_options]
