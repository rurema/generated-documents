names=overwrite_accessor
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md
since_by_name=overwrite_accessor=1.9.3
until_by_name=

### def overwrite_accessor(name){ ... } -> ()

呼び出し時に特別な動作をする必要のある属性があります。
このメソッドはそういうことを可能にします。

ブロックパラメータは任意のものを使用できます。

- **param** `name` -- 属性名を指定します。

