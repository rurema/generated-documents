names=to_hash
visibility=public
kind=defined
source_location=manual/api/dbm/DBM.md

### def to_hash -> Hash

self をハッシュに変換して返します。

```ruby
require 'dbm'
  
db1 = DBM.open('aaa.db', 0666, DBM::NEWDB)
db1[:a] = 'aaa'
db1[:b] = 'bbbbbb'
p db1.to_hash #=> {"a"=>"aaa", "b"=>"bbbbbb"}
```

