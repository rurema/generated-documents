names=inspect_sockaddr
visibility=public
kind=defined
source_location=manual/api/socket/Addrinfo.md
since_by_name=inspect_sockaddr=1.9.3
until_by_name=

### def inspect_sockaddr -> String

アドレスやポート番号などの情報を人間に読める形の文字列で返します。

```ruby
require 'socket'

p Addrinfo.tcp("localhost", 80).inspect_sockaddr   #=> "127.0.0.1:80"
p Addrinfo.tcp("ip6-localhost", 80).inspect_sockaddr #=> "[::1]:80"
p Addrinfo.unix("/tmp/sock").inspect_sockaddr      #=> "/tmp/sock"
```
