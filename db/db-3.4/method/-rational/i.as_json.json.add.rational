names=as_json
visibility=public
kind=added
source_location=manual/api/json/add/rational.md
since_by_name=as_json=1.9.3
until_by_name=as_json=4.1
rbs_sig=(uninitialized)

### def as_json(*args) -> Hash
{: since="1.9.3"}

`self` を JSON 形式の文字列に変換する際に使う、中間表現となるハッシュに変換して返します。
[m:Rational#to_json] が内部で使用しています。

ハッシュには、[m:JSON.create_id] をキーとしてクラス名が、`'n'` に分子が、
`'d'` に分母が入ります。

- **param** `args` -- 無視されます。

```ruby title="例"
require 'json/add/rational'

hash = Rational(2, 3).as_json
p hash['json_class'] # => "Rational"
p hash['n']           # => 2
p hash['d']           # => 3
```

- **SEE** [m:Rational#to_json], [m:Rational.json_create]
