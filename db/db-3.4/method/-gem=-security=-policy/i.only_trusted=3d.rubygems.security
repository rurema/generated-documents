names=only_trusted=
visibility=public
kind=defined
source_location=manual/api/rubygems/security.md
since_by_name=only_trusted=3d=1.9.3
until_by_name=

### def only_trusted=(flag)

検証済みの Gem のみインストールするかどうかを設定します。

- **param** `flag` -- 真、または偽を指定します。

