names=group
visibility=public
kind=defined
source_location=refm/api/src/_builtin/Thread

--- group    -> ThreadGroup

スレッドが属している [[c:ThreadGroup]] オブジェクトを返します。

    p Thread.current.group == ThreadGroup::Default
    # => true

