names=pipe=
visibility=public
kind=defined
source_location=manual/api/rdoc/options.md
since_by_name=pipe=3d=1.9.3
until_by_name=

### def pipe=(val)

val に true を指定した場合、コマンドライン引数の --pipe オプションと同
様の指定を行います。

- **param** `val` -- --pipe オプションと同じ指定を行う場合は true、そうでない場合
           は false を指定します。


