names=values_at
visibility=public
kind=defined
source_location=refm/api/src/yaml/dbm.rd:192

--- values_at(*keys) -> [object]

keys に対応する値を配列に格納して返します。

対応するキーが見つからなかった要素には nil が格納されます。

@param keys キーを文字列で指定します。複数指定することができます。
