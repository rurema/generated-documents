names=systmpdir
visibility=public
kind=defined
source_location=manual/api/etc/Etc.md

### module_function def systmpdir -> String | nil

システムのテンポラリディレクトリを返します。

```ruby
require 'etc'
p Etc.systmpdir # => "/tmp"
```

