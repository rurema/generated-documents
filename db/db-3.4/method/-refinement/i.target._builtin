names=target
visibility=public
kind=defined
source_location=manual/api/_builtin/Refinement.md
since_by_name=target=3.3
until_by_name=
rbs_sig=(uninitialized)

### def target -> Class | Module

`self` が [m:Module#refine] の対象にしているクラスまたはモジュールを返します。

```ruby
module M
  refine String do
  end
end

p M.refinements[0].target # => String
```

- **SEE** [m:Module#refinements], [m:Module#refine]


