names=lazy
visibility=public
kind=defined

--- lazy -> self

self を返します。

例:
  lazy = (100..Float::INFINITY).lazy
  p lazy.lazy         # => #<Enumerator::Lazy: 100..Infinity>
  p lazy == lazy.lazy # => true

