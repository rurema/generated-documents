names=absolute,absolute?
visibility=public
kind=defined
source_location=refm/api/src/uri/Generic:353

--- absolute     -> bool
--- absolute?    -> bool

自身に scheme が設定されているなら true を返します。そうでない場合は false を返します。

例:
  require 'uri'
  p URI.parse('http://example.com/').absolute?    #=> true
  p URI.parse('./').absolute?                     #=> false

