names=delete,unlink
visibility=public
kind=defined
source_location=refm/api/src/pathname.rd:1146

--- unlink -> Integer
--- delete -> Integer
self が指すディレクトリあるいはファイルを削除します。

//emlist[例][ruby]{
require "pathname"

pathname = Pathname("/path/to/sample")
pathname.exist? # => true
pathname.unlink # => 1
pathname.exist? # => false
//}

