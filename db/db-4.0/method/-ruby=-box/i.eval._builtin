names=eval
visibility=public
kind=defined
source_location=manual/api/_builtin/Ruby__Box.md
since_by_name=eval=4.0
until_by_name=

### def eval(code) -> object

文字列 `code` を Ruby のコードとして self（レシーバのボックス）の中で評価し、
その結果を返します。

ファイルを [m:Ruby::Box#load] で読み込むのと同様に、`code` は self の中で実行されます。

