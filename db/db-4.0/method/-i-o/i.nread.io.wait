names=nread
visibility=public
kind=added
source_location=manual/api/io/wait.md
since_by_name=nread=1.9.3
until_by_name=

### def nread -> Integer
ブロックせずに読み込み可能なバイト数を返します。
ブロックする場合は0を返します。

判別が不可能な場合は0を返します。

