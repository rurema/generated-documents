names=chown
visibility=public
kind=defined
source_location=manual/api/pathname.md
since_by_name=
until_by_name=
rbs_sig={"overloads":[{"segments":[["t","("],["c","Integer"],["t"," owner, "],["c","Integer"],["t"," group) -> "],["c","Integer"]],"params":["owner","group"],"arity":[2,2]}]}

### def chown(owner, group) -> Integer

File.chown(owner, group, self.to_s) と同じです。

- **param** `owner` -- オーナーを指定します。

- **param** `group` -- グループを指定します。

```ruby title="例"
require 'pathname'

p Pathname('testfile').stat.uid   # => 501
Pathname('testfile').chown(502, 12)
p Pathname('testfile').stat.uid   # => 502
```

- **SEE** [m:File.chown], [m:File#chown]

