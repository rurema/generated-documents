names=refresh
visibility=public
kind=defined
source_location=manual/api/rubygems/source_info_cache.md
since_by_name=refresh=1.9.3
until_by_name=

### def refresh(all) -> false

取得元ごとにキャッシュデータを更新します。

- **param** `all` -- 真を指定すると全てのキャッシュを更新します。そうでない場合は、最新の Gem パッケージの情報のみ更新します。

