names=requirements=
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md
since_by_name=requirements=3d=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def requirements=(informations)

この Gem パッケージを動作させるのに必要な条件をセットします。
これはユーザのためのシンプルな情報をセットします。

- **param** `informations` -- 情報を文字列の配列で指定します。

