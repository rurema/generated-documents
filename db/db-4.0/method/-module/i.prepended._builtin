names=prepended
visibility=private
kind=defined
source_location=manual/api/_builtin/Module.md
since_by_name=prepended=2.0.0
until_by_name=
rbs_sig={"overloads":[{"segments":[["t","("],["c","Module"],["t"," othermod) -> untyped"]],"params":["othermod"],"arity":[1,1]}]}

### def prepended(class_or_module) -> ()

self が [m:Module#prepend] されたときに対象のクラスまたはモジュールを引数にしてインタプリタがこのメソッドを呼び出します。

- **param** `class_or_module` -- [m:Module#prepend] を実行したオブジェクト

```ruby title="例"
module A
  def self.prepended(mod)
    puts "#{self} prepended to #{mod}"
  end
end
module Enumerable
  prepend A
end
# => "A prepended to Enumerable"
```

- **SEE** [m:Module#included], [m:Module#prepend], [m:Module#prepend_features]

