names===,eql?
visibility=public
kind=defined
source_location=manual/api/matrix/Vector.md
since_by_name=
until_by_name=

### def ==(v) -> bool
### def eql?(v) -> bool

`self` と引数 `v` を比較し、`true`/`false` を返します。

- **param** `v` -- 比較対象ベクトル

