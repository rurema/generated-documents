names===
visibility=public
kind=defined
source_location=manual/api/_builtin/Comparable.md

### def ==(other)    -> bool

比較演算子 <=> をもとにオブジェクト同士を比較します。
<=> が 0 を返した時に、true を返します。
それ以外を返した場合は、false を返します。

- **param** `other` -- 自身と比較したいオブジェクトを指定します。

```ruby title="例"
1 == 1   # => true
1 == 2   # => false
```

