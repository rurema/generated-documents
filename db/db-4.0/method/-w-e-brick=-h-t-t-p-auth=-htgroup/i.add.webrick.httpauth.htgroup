names=add
visibility=public
kind=defined
source_location=manual/api/webrick/httpauth/htgroup.md
since_by_name=
until_by_name=

### def add(group, members)

与えられたグループにメンバを追加します。

- **param** `group` -- メンバを追加するグループを指定します。

- **param** `members` -- 追加するメンバを配列で指定します。

