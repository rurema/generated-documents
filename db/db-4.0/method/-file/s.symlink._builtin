names=symlink
visibility=public
kind=defined
source_location=manual/api/_builtin/File.md
since_by_name=
until_by_name=
rbs_sig={"overloads":[{"segments":[["t","(string | _ToPath old_name, string | _ToPath new_name) -> 0"]],"params":["old_name","new_name"],"arity":[2,2]}]}

### def File.symlink(old, new)    -> 0

old への new という名前のシンボリックリンクを生成します。

シンボリックリンクの作成に成功すれば 0 を返します。失敗した場合は例外 [c:Errno::EXXX] が発生します。

- **param** `old` -- ファイル名を表す文字列を指定します。 

- **param** `new` -- シンボリックリンクを表す文字列を指定します。 

- **raise** `Errno::EXXX` -- 失敗した場合に発生します。

```ruby title="例"
p File.symlink("testfile", "testlink") # => 0
```

