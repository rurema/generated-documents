names=new
visibility=public
kind=defined
source_location=refm/api/src/rubygems/package/tar_writer.rd:80

--- new(io, limit) -> Gem::Package::TarWriter::BoundedStream

自身を初期化します。

@param io ラップする IO を指定します。

@param limit 書き込み可能な最大のサイズを指定します。

