names=wait_writable
visibility=private
kind=added
source_location=manual/api/un.md
since_by_name=wait_writable=1.9.3
until_by_name=

### def wait_writable -> ()

ファイルが書き込み可能になるまで待ちます。

```console
ruby -run -e wait_writable -- [OPTION] FILE

-n RETRY    リトライ回数
-w SEC      リトライごとに待つ秒数
-v          詳細表示
```

