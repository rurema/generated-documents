names=<<
visibility=public
kind=defined
source_location=manual/api/logger/Logger.md
since_by_name=
until_by_name=

### def <<(msg) -> Integer | nil

ログを出力します。

- **param** `msg` -- ログに出力するメッセージ。

```ruby title="例"
require 'logger'
logger = Logger.new(STDOUT)

logger << "add message"

# => add message
```

