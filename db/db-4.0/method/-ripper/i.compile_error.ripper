names=compile_error
visibility=private
kind=defined
source_location=refm/api/src/ripper.rd:124

--- compile_error(msg) -> nil

解析した Ruby プログラムの中にコンパイルエラーがあった場合に実行されま
す。

@param msg エラーメッセージ。

サブクラスでオーバライドして使用します。

