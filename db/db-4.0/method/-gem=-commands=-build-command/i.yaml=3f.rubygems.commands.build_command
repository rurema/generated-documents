names=yaml?
visibility=public
kind=defined
source_location=manual/api/rubygems/commands/build_command.md
since_by_name=yaml=3f=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def yaml?(filename) -> bool

与えられたファイル名を持つファイルが yaml ファイルである場合に真を返します。

- **param** `filename` -- ファイル名を指定します。
