names=nprocessors
visibility=public
kind=defined
source_location=manual/api/etc/Etc.md
since_by_name=nprocessors=2.2.0
until_by_name=
rbs_sig=(uninitialized)

### module_function def nprocessors -> Integer

有効な CPU コア数を返します。

```ruby
require 'etc'
p Etc.nprocessors # => 4
```

