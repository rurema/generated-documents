names=on_head
visibility=public
kind=defined
source_location=manual/api/optparse/OptionParser.md
since_by_name=
until_by_name=
rbs_sig=(uninitialized)

### def on_head(*arg, &block) -> self

オプションを取り扱うためのブロックを自身の持つリストの最初に登録します。

- **param** `arg` -- [m:OptionParser#on] と同様です。

- **param** `block` -- [m:OptionParser#on] と同様です。

```ruby title="例"
require "optparse"

opts = OptionParser.new do |opts|
  opts.on_head("-i", "--init")
  opts.on("-u", "--update")
  opts.on_tail("-h", "--help")
end

puts opts.help

# => Usage: test [options]
#    -i, --init
#    -u, --update
#    -h, --help
```

- **SEE** [m:OptionParser#on], [m:OptionParser#on_tail]

