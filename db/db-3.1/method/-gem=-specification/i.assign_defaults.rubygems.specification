names=assign_defaults
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md
since_by_name=assign_defaults=1.9.3
until_by_name=

### def assign_defaults -> ()

全ての属性にデフォルト値をセットします。

これはアクセサメソッドを使用して行われるので、ブロックを用いた特別な初期化も
きちんと実行されます。セットされる値はデフォルト値のコピーです。

