names=signing_key=
visibility=public
kind=defined
source_location=manual/api/rubygems/specification.md
since_by_name=signing_key=3d=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def signing_key=(key)

この Gem パッケージの署名に使用するキーをセットします。

- **param** `key` -- 署名に使用するキーを指定します。

