names=execute
visibility=public
kind=defined
source_location=manual/api/irb/cmd/pushws.md
since_by_name=
until_by_name=

### def execute(*obj) -> [IRB::WorkSpace]

UNIX シェルコマンドの popd と同じです。

- **param** `obj` -- 使用しません。
