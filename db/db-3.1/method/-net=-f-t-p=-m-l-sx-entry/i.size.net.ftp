names=size
visibility=public
kind=defined
source_location=manual/api/net/ftp.md
since_by_name=size=2.3.0
until_by_name=
rbs_sig=(uninitialized)

### def size -> Integer|nil

エントリのサイズを返します。

サイズ情報がない、もしくはサイズが意味をもたない(ディレクトリの場合など)
は nil を返します。

