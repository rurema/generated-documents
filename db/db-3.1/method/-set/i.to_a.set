names=to_a
visibility=public
kind=defined
source_location=manual/api/set/Set.md

### def to_a -> Array
self を配列に変換します。要素の順序は不定です。

```ruby
require 'set'
set = Set['hello', 'world']
p set.to_a # => ["hello", "world"]
```

