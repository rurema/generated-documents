names===,===,eql?
visibility=public
kind=defined
source_location=refm/api/src/bigdecimal/BigDecimal:867

--- ==(other)   -> bool
--- ===(other)  -> bool
--- eql?(other) -> bool

self が other と等しい場合に true を、そうでない場合に false を返します。

それぞれの値は [[m:BigDecimal#coerce]] で変換して比較される場合があります。

//emlist[][ruby]{
require 'bigdecimal'
BigDecimal('1.0') == 1.0 # => true
//}

