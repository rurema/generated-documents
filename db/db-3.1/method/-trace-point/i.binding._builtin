names=binding
visibility=public
kind=defined
source_location=manual/api/_builtin/TracePoint.md
since_by_name=binding=2.0.0
until_by_name=
rbs_sig=(uninitialized)

### def binding -> Binding

発生したイベントによって生成された [c:Binding] オブジェクトを返します。


```ruby title="例"
def foo(ret)
  ret
end
trace = TracePoint.new(:call) do |tp|
  p tp.binding.local_variables # => [:ret]
end
trace.enable
foo 1
```

