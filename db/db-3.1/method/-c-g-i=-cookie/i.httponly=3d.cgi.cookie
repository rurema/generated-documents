names=httponly=
visibility=public
kind=defined
source_location=manual/api/cgi/cookie.md
since_by_name=httponly=3d=3.0
until_by_name=httponly=3d=4.0
rbs_sig=(uninitialized)

### def httponly=(val)

httpオンリークッキーであるかどうかを変更します。

- **param** `val` -- 真を指定すると自身はhttpオンリークッキーになります。

