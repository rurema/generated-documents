names=writeencoding
visibility=public
kind=defined
source_location=refm/api/src/rexml/xmldecl.rd:93

--- writeencoding -> bool
XML宣言内の encoding の宣言を出力時に省略するならば真を返します。

これが真であっても UTF-8 以外のエンコーディングを指定している
ならば encoding は出力されます。


