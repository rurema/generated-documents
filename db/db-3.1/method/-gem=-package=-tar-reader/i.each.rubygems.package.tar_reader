names=each,each_entry
visibility=public
kind=defined
source_location=manual/api/rubygems/package/tar_reader/Gem__Package__TarReader.md
since_by_name=each=1.9.3,each_entry=1.9.3
until_by_name=
rbs_sig=(uninitialized)

### def each{|entry| ... }
### def each_entry{|entry| ... }

ブロックに一つずつエントリを渡して評価します。

