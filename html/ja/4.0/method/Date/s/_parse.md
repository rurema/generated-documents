# Date._parse

### def Date._parse(str, complete = true) -> Hash

このメソッドは [Date.parse](../../../method/Date/s/parse.md) と似ていますが、日付オブジェクトを生成せずに、見いだした要素をハッシュで返します。

[Date.parse](../../../method/Date/s/parse.md) も参照してください。

- **param** `str` -- 日付をあらわす文字列
- **param** `complete` -- 年を補完するか
