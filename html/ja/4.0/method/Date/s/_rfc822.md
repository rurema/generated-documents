# Date._rfc822

### def Date._rfc2822(str) -> Hash
### def Date._rfc822(str) -> Hash

このメソッドは [Date.rfc2822](../../../method/Date/s/rfc2822.md) と似ていますが、日付オブジェクトを生成せずに、見いだした要素をハッシュで返します。

[Date.rfc2822](../../../method/Date/s/rfc2822.md) も参照してください。

- **param** `str` -- 日付をあらわす文字列
