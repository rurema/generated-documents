# Gem::Specification#bin_file

### def bin_file(name) -> String
{: since="1.9.3"}

この gem に含まれる実行可能ファイル `name` へのフルパスを返します。

- **param** `name` -- 実行可能ファイル名を指定します。

- **SEE** [Gem::Specification#bin_dir](../../../method/Gem=3a=3aSpecification/i/bin_dir.md)
