# BigDecimal.json_create

### def BigDecimal.json_create(hash) -> BigDecimal

JSON のオブジェクトから [BigDecimal](../../../class/BigDecimal.md) のオブジェクトを生成して返します。

- **param** `hash` -- [Marshal?.load](../../../method/Marshal/m/load.md) 可能な値をキー 'b' に持つハッシュを指定します。
