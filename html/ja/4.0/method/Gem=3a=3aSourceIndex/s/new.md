# Gem::SourceIndex.new

### def Gem::SourceIndex.new(specifications = {}) -> Gem::SourceIndex

与えられたハッシュを元に自身を初期化します。

- **param** `specifications` -- キーを Gem の名前、値を [Gem::Specification](../../../class/Gem=3a=3aSpecification.md) のインスタンスとするハッシュを指定します。
