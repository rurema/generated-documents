# Gem::Package::TarHeader.from

### def Gem::Package::TarHeader.from(stream) -> Gem::Package::TarHeader

stream から先頭 512 バイトを読み込んで [Gem::Package::TarHeader](../../../class/Gem=3a=3aPackage=3a=3aTarHeader.md) のインスタンスを作成して返します。

- **param** `stream` -- IO のように read メソッドを持つオブジェクトを指定します。
