# JSON::State#depth=

### def depth=(depth)

データ構造のネストの深さの現在値を整数 depth にセットします。
インデントを伴う生成では、この深さを起点として出力されます。
ネストの深さの上限([JSON::State#max_nesting](../../../method/JSON=3a=3aState/i/max_nesting.md))とは別の値です。
