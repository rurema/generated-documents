# JSON::State#generate

### def generate(obj) -> String

オブジェクト obj から有効な JSON 形式の文字列を生成し、その結果を返します。
有効な JSON 形式の文字列を生成できない場合は、[JSON::GeneratorError](../../../class/JSON=3a=3aGeneratorError.md) 例外が発生します。
