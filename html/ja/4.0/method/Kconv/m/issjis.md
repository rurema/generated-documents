# Kconv?.issjis

### module_function def issjis(str) -> bool

文字列 str が Shift_JIS なバイト列として正当であるかどうかを判定します。

- **param** `str` -- 判定対象の文字列
- **SEE** [String#issjis](../../../method/String/i/issjis.md)
