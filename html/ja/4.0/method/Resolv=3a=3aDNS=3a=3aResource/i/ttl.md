# Resolv::DNS::Resource#ttl

### def ttl -> Integer
{: since=""}

この DNS リソースレコードの残り TTL(Time To Live)を返します。

[Resolv::DNS#getresource](../../../method/Resolv=3a=3aDNS/i/getresource.md) などで取得したリソースで、DNS サーバから返された TTL の値を保持しています。
