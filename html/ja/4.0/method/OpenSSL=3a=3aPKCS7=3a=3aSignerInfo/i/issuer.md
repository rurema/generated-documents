# OpenSSL::PKCS7::SignerInfo#issuer

### def issuer -> OpenSSL::X509::Name
### def name -> OpenSSL::X509::Name

署名者の証明書の発行者の名前(DN)を返します。

これと [OpenSSL::PKCS7::SignerInfo#serial](../../../method/OpenSSL=3a=3aPKCS7=3a=3aSignerInfo/i/serial.md) で署名者を一意に識別します。
