# IO#scroll_forward

### def scroll_forward(n) -> self

画面全体を n 行分、上方向にスクロールします。新しく現れた行は空白になります。

- **param** `n` -- スクロールする行数を整数で指定します。

- **SEE** [IO#scroll_backward](../../../method/IO/i/scroll_backward.md)
