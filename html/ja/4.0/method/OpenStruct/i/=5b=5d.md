# OpenStruct#[]

### def [](name) -> object

引数 name で指定した要素に対応する値を返します。

- **param** `name` -- 要素の名前を文字列か [Symbol](../../../class/Symbol.md) オブジェクトで指定します。

```ruby title="例"
require 'ostruct'
person = OpenStruct.new('name' => 'John Smith', 'age' => 70)
person[:age] # => 70, person.age と同じ
```
