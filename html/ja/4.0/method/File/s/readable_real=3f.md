# File.readable_real?

### def File.readable_real?(path)    -> bool

[FileTest?.readable_real?](../../../method/FileTest/m/readable_real=3f.md) と同じです。

- **param** `path` -- パスを表す文字列か IO オブジェクトを指定します。
