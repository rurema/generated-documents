# Gem::SourceInfoCacheEntry.new

### def Gem::SourceInfoCacheEntry.new(si, size) -> Gem::SourceInfoCacheEntry

キャッシュのエントリを作成します。

- **param** `si` -- [Gem::SourceIndex](../../../class/Gem=3a=3aSourceIndex.md) のインスタンスを指定します。

- **param** `size` -- エントリのサイズを指定します。
