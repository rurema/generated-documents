# Logger#<<

### def <<(msg) -> Integer | nil

ログを出力します。

- **param** `msg` -- ログに出力するメッセージ。

```ruby title="例"
require 'logger'
logger = Logger.new(STDOUT)

logger << "add message"

# => add message
```
