# Process.abort

### def Process.abort            -> ()
### def Process.abort(message)   -> ()

関数 [Kernel?.abort](../../../method/Kernel/m/abort.md) と同じです。

- **param** `message` -- 終了時のメッセージを文字列で指定します。
