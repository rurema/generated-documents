# Process.spawn

### def Process.spawn(cmd, *arg)    -> Integer

関数 [Kernel?.spawn](../../../method/Kernel/m/spawn.md) と同じです。

- **param** `cmd` -- [Kernel?.spawn](../../../method/Kernel/m/spawn.md) と同じです。

- **param** `arg` -- [Kernel?.spawn](../../../method/Kernel/m/spawn.md) と同じです。

- **raise** `NotImplementedError` -- メソッドが現在のプラットフォームで実装されていない場合に発生します。
