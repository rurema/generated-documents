# Shellwords.escape

### def Shellwords.escape(str) -> String

文字列を Bourne シェルのコマンドライン中で安全に使えるようにエスケープします。

このメソッドは、[Shellwords?.shellescape](../../../method/Shellwords/m/shellescape.md) の別名です。

- **param** `str` -- エスケープの対象となる文字列を指定します。
- **return** -- エスケープされた文字列を返します。
