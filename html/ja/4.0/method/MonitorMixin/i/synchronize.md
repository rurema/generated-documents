# MonitorMixin#synchronize

### def mon_synchronize { ... } -> object
### def synchronize     { ... } -> object

モニターをロックし、ブロックを実行します。実行後に必ずモニターのロックを解放します。

ブロックの評価値を返り値として返します。

- **SEE** [MonitorMixin#mon_enter](../../../method/MonitorMixin/i/mon_enter.md)
