# OpenSSL::BN#mod_sqr

### def mod_sqr(m) -> OpenSSL::BN

(self ** 2) % m を返します。

- **param** `m` -- mod を取る数
- **raise** `OpenSSL::BNError` -- 計算時エラー
- **SEE** [OpenSSL::BN#sqr](../../../method/OpenSSL=3a=3aBN/i/sqr.md)
