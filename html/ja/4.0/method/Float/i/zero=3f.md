# Float#zero?

### def zero?  -> bool

`self` がゼロの時、`true` を返します。そうでない場合は `false` を返します。

```ruby title="例"
p 10.0.zero?          # => false
p 0.zero?             # => true
p 0.0.zero?           # => true
```
