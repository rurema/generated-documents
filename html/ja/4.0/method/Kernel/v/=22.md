# Kernel$"

### gvar $"               -> [String]
### gvar $LOADED_FEATURES -> [String]

[Kernel?.require](../../../method/Kernel/m/require.md) でロードされたファイル名を含む配列です。

[Kernel?.require](../../../method/Kernel/m/require.md) で同じファイルを複数回ロードしないようにするためのロックとして使われます。

この変数はグローバルスコープです。
