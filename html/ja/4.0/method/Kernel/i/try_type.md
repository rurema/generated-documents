# Kernel#try_type

### def try_type(type, headers = nil, opt = "") -> bool
### def try_type(type, headers = nil, opt = ""){ ... } -> bool

[Kernel#have_type](../../../method/Kernel/i/have_type.md) を使ってください。

- **param** `type` -- 検査したい型の名前を指定します。

- **param** `headers` -- 追加のヘッダを指定します。

- **param** `opt` -- コンパイラに渡す追加のオプションを指定します。
