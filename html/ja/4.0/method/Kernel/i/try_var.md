# Kernel#try_var

### def try_var(var, headers = nil) -> bool
### def try_var(var, headers = nil){ ... } -> bool

[Kernel#have_var](../../../method/Kernel/i/have_var.md) を使ってください。

- **param** `var` -- 検査したい変数名を指定します。

- **param** `headers` -- 追加のヘッダを指定します。
