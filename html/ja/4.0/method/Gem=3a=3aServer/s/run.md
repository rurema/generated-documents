# Gem::Server.run

### def Gem::Server.run(options) -> Gem::Server

与えられたオプションを使用してサーバを起動します。

- **param** `options` -- オプションを表すハッシュを指定します。含まれるキーは :gemdir, :port, :daemon です。

- **SEE** [Gem::Server.new](../../../method/Gem=3a=3aServer/s/new.md)
