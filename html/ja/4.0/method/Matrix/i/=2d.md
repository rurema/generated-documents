# Matrix#-

### def -(m) -> Matrix

`self` から行列 `m` を減算した行列を返します。
`self` の列数が 1 なら [Vector](../../../class/Vector.md) オブジェクトも指定できます。

- **param** `m` -- 減算する行列。減算可能な行列やベクトルを指定します。

- **raise** `ExceptionForMatrix::ErrDimensionMismatch` -- 次元が合わない場合に発生します
