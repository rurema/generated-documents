# Matrix.row_vector

### def Matrix.row_vector(row) -> Matrix

要素がrowの(1,n)型の行列(行ベクトル)を生成します。

- **param** `row` -- (1,n)型の行列として生成する[Vector](../../../class/Vector.md) [Array](../../../class/Array.md) オブジェクト
