# Integer#<<

### def <<(bits) -> Integer

`self` を `bits` だけ左にビットシフトした整数を返します。

`Integer` オブジェクトを左項とするシフト演算子 `<<` はこのメソッドの呼び出しになります。

- **param** `bits` -- シフトさせるビット数

```ruby
printf("%#b\n", 0b0101 << 1) # => 0b1010
p -1 << 1 # => -2
```
