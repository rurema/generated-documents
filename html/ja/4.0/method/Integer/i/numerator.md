# Integer#numerator

### def numerator -> Integer

常に `self` を返します。

これは `self` を既約分数で表したときの分子（numerator）です。

```ruby
p 10.numerator  # => 10
p -10.numerator # => -10
```

- **SEE** [Integer#denominator](../../../method/Integer/i/denominator.md), [Rational#numerator](../../../method/Rational/i/numerator.md)
