# Integer#pred

### def pred    -> Integer

`self` から `1` を引いた値を返します。

メソッド名 `pred` は predecessor（前のもの）より。

```ruby
p 1.pred    # => 0
p (-1).pred # => -2
```

- **SEE** [Integer#next](../../../method/Integer/i/next.md)
