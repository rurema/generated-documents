# Integer#/

### def /(other) -> Numeric

除算の算術演算子。

`other` が `Integer` の場合、整商（整数の商）を `Integer` で返します。
普通の商（剰余を考えない商）を越えない最大の整数をもって整商とします。

`other` が [Float](../../../class/Float.md)、[Rational](../../../class/Rational.md)、[Complex](../../../class/Complex.md) の場合、普通の商を `other` と同じクラスのインスタンスで返します。

- **param** `other` -- 二項演算の右側の引数(対象)
- **return** -- 計算結果

```ruby title="例"
p 7 / 2 # => 3
p 7 / -2 # => -4
p 7 / 2.0 # => 3.5
p 7 / 2r # => (7/2)
p 7 / (2+0i) # => ((7/2)+0i)

begin
  2 / 0
rescue => e
  e # => #<ZeroDivisionError: divided by 0>
end
```

- **SEE** [Integer#div](../../../method/Integer/i/div.md), [Integer#fdiv](../../../method/Integer/i/fdiv.md), [Numeric#quo](../../../method/Numeric/i/quo.md)
