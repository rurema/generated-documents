# library dbm

DBM を Ruby スクリプトから扱えるようにするライブラリです。

扱えるキーや値のサイズはリンクしているライブラリに依存します。

作成されるデータベースはアーキテクチャに依存するため、作成した環境と異なる環境ではデータベースが読み込めない可能性があります。

- **SEE** [gdbm](../library/gdbm.md), [sdbm](../library/sdbm.md), [man:dbm(3)]
